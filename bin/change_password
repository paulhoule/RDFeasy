#!/bin/sh

#
# set_dba_password old_password new_password
#

export OLDPW=$1
export NEWPW=$2

isql <<EOF
   CHANGE_PASSWORD('dba','$OLDPW','$NEWPW');
   UPDATE WS.WS.SYS_DAV_USER set U_PASSWORD='$NEWPW';
EOF
