#!/bin/bash

#
# genpasswd() function from
#
# http://www.cyberciti.biz/faq/linux-random-pssword-generator/
#

genpasswd() {
   local l=$1
   [ "$l" == "" ] && l=16
   tr -dc A-Za-z0-9_ < /dev/urandom | head -c ${l} | xargs
}

genpasswd
